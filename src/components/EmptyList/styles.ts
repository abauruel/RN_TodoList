import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emptyContainer: {
    alignItems: "center",
    borderTopWidth: 1,
    borderColor: "#808080",
    width: "100%",
    marginTop: 20,
  },
  clipboard: {
    width: 56,
    height: 56,
    marginTop: 48,
  },
  mainTextEmpty: {
    marginTop: 16,
    color: "#808080",
    fontWeight: "bold",
  },
  subTextEmpty: {
    color: "#808080",
  },
});
